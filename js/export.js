export function audioBufferToWav(buffer){const channels=buffer.numberOfChannels,rate=buffer.sampleRate,length=buffer.length*channels*2+44,ab=new ArrayBuffer(length),view=new DataView(ab);let o=0;const ws=s=>{for(let i=0;i<s.length;i++)view.setUint8(o++,s.charCodeAt(i))};ws('RIFF');view.setUint32(o,length-8,true);o+=4;ws('WAVEfmt ');view.setUint32(o,16,true);o+=4;view.setUint16(o,1,true);o+=2;view.setUint16(o,channels,true);o+=2;view.setUint32(o,rate,true);o+=4;view.setUint32(o,rate*channels*2,true);o+=4;view.setUint16(o,channels*2,true);o+=2;view.setUint16(o,16,true);o+=2;ws('data');view.setUint32(o,length-44,true);o+=4;for(let i=0;i<buffer.length;i++){for(let ch=0;ch<channels;ch++){let s=Math.max(-1,Math.min(1,buffer.getChannelData(ch)[i]));view.setInt16(o,s<0?s*0x8000:s*0x7fff,true);o+=2}}return new Blob([ab],{type:'audio/wav'})}
export function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.append(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},1000)}
export async function renderWav(sourceBuffer,settings,mode='wet'){if(!sourceBuffer)throw new Error('No audio loaded');const ctx=new OfflineAudioContext(sourceBuffer.numberOfChannels,sourceBuffer.length,sourceBuffer.sampleRate);const src=ctx.createBufferSource();src.buffer=sourceBuffer;let node=src;if(mode!=='dry'){const hp=ctx.createBiquadFilter();hp.type='highpass';hp.frequency.value=75;const comp=ctx.createDynamicsCompressor();comp.threshold.value=-18;comp.ratio.value=3.5;const shelf=ctx.createBiquadFilter();shelf.type='highshelf';shelf.frequency.value=8000;shelf.gain.value=2;const gain=ctx.createGain();gain.gain.value=(settings.outputGain??95)/100;node.connect(hp).connect(comp).connect(shelf).connect(gain).connect(ctx.destination)}else node.connect(ctx.destination);src.start();return audioBufferToWav(await ctx.startRendering())}

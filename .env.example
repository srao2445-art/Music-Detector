PORT=3000
NODE_ENV=development
SESSION_SECRET=replace-with-a-long-random-secret
DATABASE_PATH=./data/creator-studio.db
# Required only on the first run if the database has no admin user.
# Set this to the initial admin password through your host's secret manager or local .env.
INITIAL_ADMIN_PASSWORD=
INITIAL_ADMIN_EMAIL=srao2445@gmail.com

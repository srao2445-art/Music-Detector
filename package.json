{
  "name": "the-creator-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A portfolio website with a secure SQLite-powered CMS dashboard.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "init-db": "node scripts/init-db.js",
    "check": "node --check app.js && node --check db.js && node --check scripts/init-db.js && node --check public/js/main.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^11.8.1",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^1.4.5-lts.1"
  }
}
